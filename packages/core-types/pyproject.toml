[project]
name = "stockimformation-types"
version = "0.1.0"
description = "Shared extension protocols for stockImformation"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
