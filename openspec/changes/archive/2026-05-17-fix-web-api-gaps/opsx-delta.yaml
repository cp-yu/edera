# OPSX Delta — fix-web-api-gaps

MODIFIED:
  runtime-config-editing:
    domain: config
    delta: 新增 GET /api/config/portfolio 和 GET /api/config/system 专用读取端点
  node-graph-dag-editor:
    domain: web
    delta: 新增 POST /api/graph/dag/{name}/nodes 端点，支持从 UI 创建节点并加入 DAG
  sources-monitor-ui:
    domain: web
    delta: 添加 API 错误状态展示和 mutation 错误反馈
