schema_version: 1
MODIFIED:
  capabilities:
    - id: cap.core.rig-cli
      intent: Rig CLI gRPC client，gRPC client 层只读取环境变量 PEM；文件 fallback 由 CLI 入口针对 human-facing 子命令显式注入；引入 force_insecure 标志解耦 bootstrap 与 mTLS 路径
