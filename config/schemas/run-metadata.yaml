display_name: Run Metadata
business_id_field: run_id
display_template: "{run_id}"
storage_tier: memory
system_protected: true
schema:
  type: object
  required:
    - run_id
  properties:
    run_id:
      type: string
    warnings:
      type: array
    events:
      type: array
validate: true
