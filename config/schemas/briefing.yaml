display_name: Briefing
business_id_field: run_id
display_template: "{run_id}"
storage_tier: database
schema:
  type: object
  required:
    - run_id
    - content
  properties:
    run_id:
      type: string
    content:
      type: string
    metadata:
      type: object
validate: true
