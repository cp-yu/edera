name: uzi-skill
version: 0.1.0
type: workflow_extension
handlers:
- name: legacy-script-adapter
  entry: adapter.py
  role: processor
  input_type: Any
  output_type: Any
imports:
  entities:
  - entities/**/*.yaml
